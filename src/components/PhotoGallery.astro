---
import PhotoPreview from "./PhotoPreview.astro";
const { photos } = Astro.props;
---

<div class="grid gap-4 grid-cols-1 md:grid-cols-2">
  {photos.map((p) => (
    <div>
      <PhotoPreview photo={p} link={`/photos/${p.id}`} />
    </div>
  ))}
</div>
