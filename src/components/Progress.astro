---
interface Props {
  value: number;
  total: number;
  valueDetail?: string;
  totalDetail?: string;
}
const { value, total, valueDetail, totalDetail } = Astro.props;
---

<div>
  <div class="border-flash border-2 rounded h-4">
    <div class="bg-flash h-full" style={{ width: `${(value / total) * 100}%` }}>
    </div>
  </div>
  <div class="flex flex-row mt-1">
    <div class="flex-grow">
      <p>{valueDetail}</p>
    </div>
    <div>
      <p>{totalDetail}</p>
    </div>
  </div>
</div>
